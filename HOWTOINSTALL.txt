- Hay que añadir hugo:
sudo apt install hugo

- Hay que añadir academic, para añadir bibtex nuevos:
pip3 install -U academic

- Hay que instalar el lftp para subir al servidor:
sudo apt install lftp

- Hay que añadir la key ssh a github en caso de no tenerla:

